#include <vector>
#include <algorithm> 
class Production{
public:

	int testSize(std::vector<int> v);
	bool testRecurring(std::vector<int> v);
	bool testRecurringVal(std::vector<int> v, int val);
	bool testRange(std::vector<int> v);
	bool testRangeVal(int val);

};
